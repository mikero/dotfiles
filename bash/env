export EDITOR="/usr/bin/vim"
export PATH=$PATH:~/bin:/usr/local/bin/:/usr/local/mysql/bin:/usr/local/ruby/bin
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"  # loads RVM into a shell session
